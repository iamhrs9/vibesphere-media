import React from 'react';

import PlaceholderScreen from '../components/PlaceholderScreen';

export default function ProfileScreen(props) {
  return (
    <PlaceholderScreen
      {...props}
      title="Profile"
      subtitle="Profile placeholder. Staff profile, settings, password updates, and photo updates will be implemented here."
    />
  );
}
